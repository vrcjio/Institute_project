/** @type {import('next').NextConfig} */
const nextConfig = {}
module.exports = {
    images: {
      domains: ['https://i.dummyjson.com','https://picsum.photos'],
    },
  };
module.exports = nextConfig
